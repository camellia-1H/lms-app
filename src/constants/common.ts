const LIMIT_DATA_QUERY = 3;

const LIMIT_QUERY_REVIEWS_FRONT = 4;

const LIMIT_QUERY_REVIEWS = 5;

const LIMIT_MAX = 10;

const ROLE_USER = {
  RHV: 'RHV',
  RGV: 'RGV',
  ROP: 'ROP',
};

const VERIFY_FLAG = {
  NOT_VERIFY: '0',
  VERIFIED: '1',
};

const COURSE_STATUS = {
  DEFAULT: 0,
  PENDING: 1,
  PUBLIC: 2,
  REJECT: 3,
};

const REQUEST_TEACHER = {
  DEFAULT: 0,
  PENDING: 1,
  ACCEPT: 2,
  REJECT: 3,
};

const FLAG_REQUEST = {
  DELETE: 'delete',
  UPDATE: 'update',
  ACCEPT: 'accept',
  REJECT: 'reject',
  ADMIN_UNPUBLIC: 'unpublic',
};

const PACKAGE_TYPE = {
  BASIC: 'packageBasic',
  PRO: 'packagePro',
};

const PAYMENT_STATUS = {
  CANCEL: 0,
  SUCCESS: 1,
};

const PAYMENT_TYPE = {
  COURSE: 0,
  PACKAGE: 1,
};

const PACKAGE_PRO_PRICE = 2;

const LIMIT_COURSE_BASIC = 3;

export {
  LIMIT_DATA_QUERY,
  LIMIT_QUERY_REVIEWS_FRONT,
  LIMIT_QUERY_REVIEWS,
  LIMIT_MAX,
  ROLE_USER,
  VERIFY_FLAG,
  COURSE_STATUS,
  REQUEST_TEACHER,
  FLAG_REQUEST,
  PACKAGE_TYPE,
  PAYMENT_STATUS,
  PAYMENT_TYPE,
  PACKAGE_PRO_PRICE,
  LIMIT_COURSE_BASIC,
};
